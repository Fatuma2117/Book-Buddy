import { useHistory } from 'react-router-dom';
function Book({ook}){


// console.log(kid.id)
    return(
        <div >
            <h3>{book.title} 
            {book.author} 
            {book.publish_year}
            {book.book_url}
            {book.points}
            {book.total_pages}

            
            
            </h3>
        </div>
    )
}
export default Book;